services:
  - type: web
    name: cybix-v1
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false